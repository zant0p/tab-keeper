{
  "type": "object",
  "properties": {
    "primaryUrl": {
      "type": "string",
      "description": "Primary target URL to keep active. Extension will always switch back to this tab after inactivity."
    },
    "secondaryUrl": {
      "type": "string",
      "description": "Secondary target URL to monitor and auto-reopen if closed."
    },
    "timerMinutes": {
      "type": "integer",
      "minimum": 1,
      "maximum": 60,
      "description": "Minutes of inactivity before switching back to primary tab (1-60)."
    },
    "autoLoginEnabled": {
      "type": "boolean",
      "description": "Enable automatic login when target tabs are opened."
    },
    "username": {
      "type": "string",
      "description": "Username/email for auto-login."
    },
    "password": {
      "type": "string",
      "description": "Password for auto-login (encrypted by Chrome)."
    }
  }
}
